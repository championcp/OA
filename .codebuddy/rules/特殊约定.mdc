# 特殊约定

这是一个规则文件，用于帮助 AI 理解您的代码库和遵循项目约定。
1.	原型模式
	•	快速生成 demo 时，可跳过测试和日志模块，但必须显式手动指定启用此规则。
2.	替代技术栈
	•	若用户明确要求，可切换数据库为 MySQL，或后端改为 NestJS / FastAPI，但默认不允许。
3.	性能调优模式
	•	仅在用户明确要求时，启用性能优化（如缓存、队列、CDN），避免初期过度设计。